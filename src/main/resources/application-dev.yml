spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:file:./db
    username: test
    password: password
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true