spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:file:./db
    username: test
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true