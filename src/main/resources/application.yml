spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:file:./db # в корне проект файл бд db.mv.db
    username: test         # имя пользователя консоли
    password: password     # пароль консоли
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true